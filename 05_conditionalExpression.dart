//conditional Expression

void main() {
  // var kingof_forest = true;
  // var animal_name = kingof_forest ? 'Lion' : 'Tiger';
  // print(animal_name);

  // var kingof_forest = false;
  // var animal_name = kingof_forest ? 'Lion' : 'Tiger';
  // print(animal_name);

  // var name = null;
  // var user = name ?? 'Guest';
  // print(user);

  var name = 'Redwan';
  var user = name ?? 'Guest';
  print(user);
}
